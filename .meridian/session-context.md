## 2026-01-24 — v0.0.55 to v0.0.56 released

**v0.0.55**: Released (was committed but tag/release missing). Fix for plan-file-sync timing.

**v0.0.56**: Periodic reminder improvements:
- Default interval: 10 → 20 actions
- Format: `[REMINDER]:` → `<system_reminder>` XML tags
- Updated in both `periodic-reminder.py` and `config.yaml`
